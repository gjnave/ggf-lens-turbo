# Disclaimer

This package downloads the full `ggf-lens-turbo` application from GitHub during setup,
then installs Python dependencies, Microsoft Lens, model weights, and GPU kernel files.

You need:
- Git
- Python 3.11
- A CUDA-capable NVIDIA GPU and working driver for the intended fast local workflow

Model files and Python packages are large and may take time to download.
